tables:
  users:
    database: seeder
    mods:
      - fake
      - test
    name: users
    rowQuantity: 5
    primaryKey: id
    columns:
      - name: id
        value: "auto_increment"
      - name: login
        value: "faker.email()"
      - name: password
        value: "faker.word()"
    relations:
      - name: role_id
        database: seeder
        table: roles
        column: id
        type: manyToOne