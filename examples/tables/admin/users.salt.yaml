tables:
  user_salt:
    database: seeder
    mods:
      - fake
      - test
    name: users_salt
    rowQuantity: "identical(seeder, users)"
    primaryKey: id
    columns:
      - name: id
        value: "increment()"
      - name: id_user
        value: "relation('oneToOne', 'seeder', 'users', 'id')"

