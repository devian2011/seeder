tables:
  user_salt:
    database: seeder
    mods:
      - fake
      - test
    name: users_salt
    rowQuantity: "identical(seeder, users)"
    primaryKey: id
    columns:
      - name: id
        value: "increment"
      - name: user_id
        value: "relation"
    relations:
      - name: user_id
        database: seeder
        table: user
        column: id
        type: oneToOne

