tables:
  users:
    database: seeder
    mods:
      - fake
      - test
    name: users
    rowQuantity: "100"
    primaryKey: id
    columns:
      - name: id
        value: "increment"
      - name: login
        value: "faker.email()"
      - name: password
        value: "faker.text()"