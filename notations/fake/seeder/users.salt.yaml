tables:
  users_salt:
    database: seeder
    name: users_salt
    rowQuantity: 5
    primaryKey: id
    columns:
      - name: id
        value: "auto_increment"
    relations:
      - name: user_id
        database: seeder
        table: users
        column: id
        type: oneToOne

